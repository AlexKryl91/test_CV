import './header.css';

const Header = () => {
  return `
    <header class="header">
      <button class="download-btn" type="button">Download PDF</button>
    </header>
  `;
};

export default Header;
